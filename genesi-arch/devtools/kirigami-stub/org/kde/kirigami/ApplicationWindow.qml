// Dev stub for Kirigami.ApplicationWindow. The app sets globalDrawer: null and
// otherwise treats it as a plain Window with children, so that is all this is.
import QtQuick
import QtQuick.Controls as QQC2
QQC2.ApplicationWindow {
    property var globalDrawer: null
    property var contextDrawer: null
    property var pageStack: null
    visible: true
}
