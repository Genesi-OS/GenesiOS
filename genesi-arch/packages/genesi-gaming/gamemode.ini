[general]
# GameMode remains available for games/tools that explicitly request it, but
# Genesi's default `genesi-game` wrapper uses game-performance. Avoid renicing
# here because ananicy-cpp already owns process priority policy.
renice=0
softrealtime=off
desiredgov=performance
