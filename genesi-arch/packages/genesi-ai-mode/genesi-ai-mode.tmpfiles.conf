# Genesi AI Mode runtime directory.
# World-writable so the panel launcher / widget (running as the user) can drop
# the `force` control file, while genesi-aid (root) publishes `state.json`.
# Lives on tmpfs, so it resets cleanly on every boot.
d /run/genesi-ai-mode 0777 root root -
